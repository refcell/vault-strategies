{
  "name": "vault-strategies",
  "license": "AGPL-3.0-only",
  "version": "1.0.0",
  "description": "Basic Strategies for Rari Capital Vaults.",
  "files": [
    "src/**/*.sol"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/abigger87/vault-strategies.git"
  },
  "devDependencies": {
    "prettier": "^2.3.1",
    "prettier-plugin-solidity": "^1.0.0-beta.13"
  },
  "scripts": {
    "lint": "prettier --write src/*.sol && prettier --write src/**/*.sol",
    "lint:check": "prettier --check src/*.sol && prettier --check src/**/*.sol"
  }
}
